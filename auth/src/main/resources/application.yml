server:
  port: 9100

spring:
  application:
    name: maxs-auth
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: 63deb4d5-bd36-4b05-9d7c-0cc579e5002f
      config:
        enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/maxs?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root

# 对称密钥，资源服务使用该密钥解密
jwt:
  access-token:
    signing-key: maxs123